---
title: " "
output: html_notebook
---



```{r set-global-options, echo=FALSE, message=FALSE, warning=FALSE}
knitr::opts_chunk$set(eval = TRUE, 
                      echo = FALSE, 
                      cache = FALSE,
                      warning =FALSE,
                      message = FALSE,
                      include = TRUE,
                      collapse = FALSE,
                      dependson = NULL,
                      engine = "R", # Chunks will always have R code, unless noted
                      error = FALSE)                     

```



```{r}
library(tidyverse)
library(ggplot2)
library(scales)
library(plotly)

```






### Análisis de la producción 

##### 1.	Producción en las Sedes.^[
*Nota:* dado que las sedes con más investigadores generan un volumen global mayor de producción, un indicador de desempeño que controla por el tamaño de cada sede es la producción per cápita por sede. Este indicador se calcula con la Suma del puntaje por sede entre el número de investigadores de la sede en cuestión. Este indicador permite contrastar el desempeño real en cada sede.]


```{r, echo=FALSE,warning=FALSE,message=FALSE}
tres<-read.csv('tres.csv')
ngls<-filter(tres,Sede=="Nogales")

pts<-group_by(tres,date,Sede)%>%
  summarize(Inv_sede=n(),Pts_sede=sum(pts_prod))%>%
  mutate(PtsPerInv=round(Pts_sede/Inv_sede,2))%>%
  arrange(desc(PtsPerInv))%>%
  group_by(Sede)%>%
  summarize(PtsPerInvm3=mean(PtsPerInv))%>%
  ungroup()%>%
mutate(Sede=fct_reorder(Sede,PtsPerInvm3))


ggplot(pts,aes(Sede,PtsPerInvm3, fill=Sede))+
  geom_col()+
  coord_flip()+
  scale_fill_brewer(palette="Oranges")+
  labs(y="Puntos por investigador", x="Sede",title="Puntuación por sede Según número de investgadores", subtitle="SIEVA Histórico 3 años 2017,2018,2019")+
  theme_light()+
  guides(fill=FALSE)



ggsave("pts_sede3.jpg", width = 25, height = 17, units = "cm",dpi=300)
```

La producción promedio por investigador en la Sede Nogales duplica la producción de Tijuana equivale  a la producción conjnta de las sedes Juárez y Monterrey  y  representa 5 veces la producción de sedes como Mexicali. 



##### 2. Calidad vs. Cantidad de producción


Un argumento a menudo enfatizado es la importancia de generar producción de calidad en contraste con un gran volumen de productos.

Para abordar, este punto se analiza a continuación la participación de los investigadores en la generación de publicaciones tipo A, la calificación de calidad más alta establecida en la regulación vigente  y se hace el contraste por departamento.

```{r, echo=FALSE,warning=FALSE,message=FALSE}
tres<-read.csv('tres.csv')

Adep<-group_by(tres,date,Departamento)%>%
  summarize(A=sum(Artículos_A))%>%
  arrange(desc(A))%>%
  filter(A!=0)


AA<-group_by(tres,date,Departamento)%>%
  summarize(inv=n())%>%
  arrange(desc(inv))%>%
  group_by(Departamento)%>%
  left_join(Adep)%>%
  mutate(rat=A/inv)%>%
  ungroup()%>%
  group_by(Departamento)%>%
  summarize(artA=round(mean(rat),2))


i<-group_by(tres,date,Departamento)%>%
  summarize(inv=n())%>%
  arrange(desc(inv))%>%
  group_by(Departamento)%>%
  left_join(Adep)%>%
  summarise(w_mean = round(weighted.mean(A,inv)))


pa<-ggplot(i, aes(fct_reorder(Departamento,w_mean), w_mean, fill=Departamento))+
  geom_col()+ 
  coord_flip()+
  labs(y="Artículos", subtitle="Por departamento académico, Histórico SIEVA, 2017,2018,2019", x="Departamento",title="Artículos tipo A.")+
  theme_light()+
  annotate("text", x="DEUMA", y=5, label="3")+
  annotate("text", x="DEP", y=5, label="3")+
  annotate("text", x="DEC", y=5, label="1")+
  annotate("text", x="DEAP", y=5, label="0.45")+
  annotate("text", x="DES", y=5, label="1")+
  annotate("text", x="DEE", y=5, label="2")

  
  

ggsave("Adep3.jpg", width = 25, height = 17, units = "cm",dpi=300)

ggplotly(pa)

```


La estimación controla por la variación en el número de investigadores por departamento de un año a otro.






¿Cómo se distribuye la producción de este tipo de artículos  de calidad  al interior de los departamentos? 
```{r, echo=FALSE,warning=FALSE,message=FALSE}

tres<-read.csv('tres.csv')

A<-group_by(tres,date,Nombre, Departamento)%>%
  summarize(total=sum(Artículos_A))%>%
  arrange(desc(total))%>%
  filter(total!=0)%>%
  ungroup()%>%
  group_by(Nombre, Departamento)%>%
summarize(A=round(mean(total)))  
  
a<-A%>%ungroup()%>%
  group_by(Departamento)%>%
  mutate(pr=round(A/sum(A),3)*100)%>%
  arrange(desc(pr))%>%
  mutate(cs= cumsum(pr))%>%
  filter(Departamento=="DEP")
  
aaa<-a%>%group_by(Departamento)%>%
  summarize(n())


Aplt<-ggplot(A,aes(fct_reorder(Nombre,A),A, fill=Departamento))+
geom_col()+ 
scale_fill_brewer(palette="Set2")+ 
  coord_flip()+
  labs(y="Artículos", subtitle="Histórico SIEVA 2017,2018,2019", x="Nombre del investigador",title="Publicaciones tipo A por investigador")+
  theme_light()

ggsave("A-investigador.jpg", width = 25, height = 17, units = "cm",dpi=300)
  
ggplotly(Aplt)

NOA<-select(tres,Nombre, date,Departamento, Artículos_A)%>%
  filter(Artículos_A==0)%>%
  group_by(Nombre)%>%
  summarize(no=n())%>%
  filter(no==3)


```

La mayoría de los investigadores generaron publicaciones tipo A en el periodo.  $66\%$,  (57/86).

Uno de cada tres investigadores en El Colef  ($33\%$) no produjeron artículos de calidad tipo A en los últimos tres periodos evaluados 29/86.


Además, la gráfica muestra que de hecho la producción de artículos tipo A durante el periodo evaluado se concentra en un número reducido de investigadores: 

Por ejemplo en el DEP, el departamento que generó el mayor número de publicaciones A en este periodo, son 8 investigadores los que generaron 
 68% del total.
 
Podría ser tentador argumentar que: dado que el DEUMA y DEP son los departamentos con el mayor número de investigadores evaluados  (22 y 21 miembros) respectivamente, esto determina la posición relativa en la generación de artículos de calidad tipo A.


No obstante, este argumento no necesariamente es válido. Para demostrar este punto se presenta a continuación la producción en artículos tipo B, C y capítulos de libro por departamento.




```{r , echo=FALSE,warning=FALSE,message=FALSE }


b<-tres%>%
  mutate(q2=Artículos_B+Artículos_C+cap_Aut+cap_Coaut.)%>%
  group_by(date,Departamento)%>%
  summarize(b=sum(q2))%>%
  arrange(desc(b))%>%
  filter(b!=0)

inve<-tres%>%
  mutate(q2=Artículos_B+Artículos_C+cap_Aut+cap_Coaut.)%>%
  group_by(date,Departamento)%>%
  summarize(inv=n())%>%
  arrange(desc(inv))%>%
  left_join(b)%>%
  mutate(bxnv=b/inv)%>%ungroup()%>%
  group_by(Departamento)%>%
  summarize(bmed=round(mean(bxnv),2))
  

b<-tres%>%
  mutate(q2=Artículos_B+Artículos_C+cap_Aut+cap_Coaut.)%>%
  group_by(date,Departamento)%>%
  summarize(inv=n())%>%
  arrange(desc(inv))%>%
  group_by(Departamento)%>%
  left_join(b)%>%
  summarise(w_mean = round(weighted.mean(b,inv)))


bp<-ggplot(inve, aes(fct_reorder(Departamento,bmed), bmed, fill=Departamento))+
  geom_col()+ 
  coord_flip()+
  labs(y="Publicaciones", subtitle="Promedio por investigador 2017,2018,2019", x="Departamento",title="Artículos tipo B,C y capítulos de libro")+
  theme_light()

ggsave("b_c_caps3.jpg", width = 25, height = 17, units = "cm",dpi=300)

bp

```


  
DEAP y DES los líderes en este tipo de productos.



```{r, echo=FALSE,warning=FALSE,message=FALSE}

tres<-read.csv('tres.csv')

l<-group_by(tres,date,Departamento)%>%
  summarize(l=sum(L_Autor))%>%
  arrange(desc(l))%>%
  filter(l!=0)


la<-group_by(tres,date,Departamento)%>%
  summarize(inv=n())%>%
  arrange(desc(inv))%>%
  group_by(Departamento)%>%
  left_join(l)%>%
  filter(!is.na(l))%>%
  mutate(ratio=l/inv)%>%
  ungroup()%>%
  group_by(Departamento)%>%
  summarize(l=sum(ratio))

## la muestra cuantos libros por investigador por departamento en los 3 años sumados. Dado que la producción de libros es ínfima (los valores no llegan a 1), es necesario crear mecanísmos para incentivar la producción de libros. Un rubro que hoy debe incentivarse con acciones claras.  

libros<-group_by(tres,date,Departamento)%>%
  summarize(inv=n())%>%
  arrange(desc(inv))%>%
  group_by(Departamento)%>%
  left_join(l)%>%
  filter(!is.na(l))%>%
  summarise(libros=sum(l))
 
  lib.media<-group_by(tres,date,Departamento)%>%
  summarize(inv=n())%>%
  arrange(desc(inv))%>%
  group_by(Departamento)%>%
  left_join(l)%>%
  filter(!is.na(l))%>%
  summarise(w_mean = round(weighted.mean(l,inv)))%>%
  left_join(libros)  


l2<-ggplot(lib.media, aes(fct_reorder(Departamento,libros), libros, fill=Departamento))+
  geom_col()+
  coord_flip()+
  labs(y="Libros", subtitle="Por departamento académico, Histórico SIEVA, 2017,2018,2019", x="Departamento",title="Libros de Autor")+
  theme_light()+
  annotate("text", x="DEUMA", y=5, label="1")+
  annotate("text", x="DEP", y=5, label="1")+
  annotate("text", x="DEC", y=5, label="2")+
  annotate("text", x="DEAP", y=5, label="2")+
  annotate("text", x="DES", y=5, label="1")+
  annotate("text", x="DEE", y=5, label="2")

l2
ggsave("libros3.jpg", width = 25, height = 17, units = "cm",dpi=300)
```
La producción de libros es ínfima, esto se percibe como **area de oportunidad**, los libros son importantes y son proyectos de más largo plazo, la inversión de tiempo necesaria para generar un libro es mayor.

Importante considerar revalorar este rubro para incentivar su producción.

Es un tipo de investigación que es mas común en DEC y DEAP. Importante fomentar el trabajo que se realiza en todos los departamentos y promover las fortalezas metodológicas propias de cada disciplina.



### 3. Mujeres y Hombres en El Colef.



```{r, echo=FALSE,warning=FALSE,message=FALSE}

tres<-read.csv('tres.csv')

pts_hrs<-group_by(tres,Nombre, Sexo)%>%
summarize(pts=mean(pts_prod),hrs=mean(Hrs_cls))%>%
  mutate(cuadrante=ifelse(pts>=6&hrs>=78,1,
                   ifelse(pts>=6&hrs<78,4,
                   ifelse(pts<6&hrs<78,3,2))))

cuadrantes<-group_by(pts_hrs,cuadrante)%>%
  summarize(total=n())

write.csv(pts_hrs,"pts_hrs.csv")

cat<-c("Díaz Carnero Emiliano Ignacio","Medina Parra Rosa Isabel", "Peña Muñoz, Jesús Javier","Pérez Caballero Jesús","Ley Cervantes, Melissa","Rosales Martínez, Yetzi", "Portillo Tellez, María del Carmen", "Hernández López, Ana Gabriela")

cat<-as.data.frame(cat)


lista<-read.csv("nuevalista.csv")%>%
    select(-X,-n)

c3<-filter(pts_hrs,cuadrante==3)%>%
  left_join(lista)%>%
  mutate(cdra=ifelse(Nombre%in%cat$cat,1,0))


c3cat<-filter(pts_hrs,cuadrante==3)%>%
  left_join(lista)%>%
  mutate(cdra=ifelse(Nombre%in%cat$cat,1,0))%>%
  filter(cdra==1)


c3dep<-filter(pts_hrs,cuadrante==3)%>%
  left_join(lista)%>%
  group_by(Departamento)%>%
  count()

c3t<-filter(pts_hrs,cuadrante==3)%>%
  left_join(lista)%>%
  mutate(cdra=ifelse(Nombre%in%cat$cat,1,0))%>%
  filter(Sede=='Tijuana')%>%
  group_by(Departamento)%>%
  count()%>%ungroup()%>%
  mutate(t=sum(n),p=round((n/t),2))

  
write.csv(c3t,"c3tj.csv")  

# Un reto principal es el programa de cátedras en sede regionales con 6 de  las 7 catedras en este perfil de producción. Adscritos en el DEC yel DEP.

# la mayoria de c3 estan en Tijuana.  Entre los investigadores NO catedras  que estan en c3, el 56%,  (18 de 32),pertenecen a la  sede Tijuana, adcritos al DEC 28%, DEP 22% y DEUMA 22%, 11% DEE y DES, DEAP 6% 
      

# ¿Qué proporción representan los investigadores en c3 por departamento?
# 53% para DEC, 47% DEP, 45%, DEAP,33%  DEUMA 30% DEE 25% y 18% DES. 
    
idep<-group_by(tres,date,Departamento)%>%
  summarize(inv=n())%>%
  arrange(desc(inv))%>%
  group_by(Departamento)%>%
  summarise(medinv=round(mean(inv)))%>%
  left_join(c3dep)%>%
  mutate(baja=round((n/medinv),2))%>%
  arrange(desc(baja))%>%
  mutate(alta=1-baja,baja=-1*baja)%>%
  select(Departamento,alta,baja)%>%
  gather(type,p,alta:baja)
  

write.csv(idep,"idep.csv")
```



```{r, echo=FALSE,warning=FALSE,message=FALSE, fig.height = 6, fig.width =8}

pts_hrs<-read.csv('pts_hrs.csv')%>%
  select(-X)
summary(pts_hrs)

plt<-ggplot(pts_hrs, aes(pts,hrs, colour=Sexo, label=Nombre))+
  scale_colour_manual(values=c("deepskyblue3", "deeppink"))+
  geom_point(aes(size=pts),alpha=.5)+
  theme_light()+
  stat_smooth( method=lm, se=FALSE)+
labs(y="Horas clase", x="Publicaciones (pts)",subtitle="Evalaución 2018", x="",title="Relación Docencia-Investigación")+
labs(size="Puntos")+
geom_hline(yintercept=78,linetype="dotted", size=.2, color="blue")+
geom_vline(xintercept=6,linetype="dotted", size=.2, color="red")+
annotate("text", x=25, y=230, label="Alta productividad")

ggplotly(plt)

ggsave("D-I.jpg", width = 25, height = 17, units = "cm",dpi=300)

```



Considerando dos de las actividades sustantivas establecidas en la regulación vigente de la institución, podemos observar que los investigadores que más publican son los que imparten más clases, (pendiente positiva en la línea de la gráfica) sin embargo, son las **mujeres** quienes participan más en **Docencia**, (la diferencia es estadísticamente significativa). 

Considerando la carga docente vemos que las investigadoras en promedio impartieron en el periodo evaluado **78 horas**, lo que se traduce en una importante **área de oportunidad**, ya que esto implica la impartición de prácticamente un curso completo de 45hrs.  por año! 


En cuanto a la generación de publicaciones por sexo no se observa una diferencia estadísticamente significativa entre la producción promedio de Hombres (7 pts promedio) y Mujeres (6 pts). 

Este rasgo del perfil de la producción de publicaciones refleja  la distribución balanceada de la planta académica (evaluada) de El Colef por sexo. 53 Hombres (55%) 42, Mujeres (45%).

 
A partir del desemepño en docencia y publicaciones Se definen 4 cuadrantes: 

+ C1 Alta productividad-Docencia, 
+ C2 Alta docencia-Baja producción
+ C3 Baja docenca-poducción. 
+ c4 Alta productividad-Baja docencia.

Siendo el c3 el que presenta una área de oportunidad para mejorar el compromiso institucional.


Adicionalmente la gráfica permite identificar que Un reto principal es el programa de cátedras en sedes regionales con 6 de  las 7 catedras con el  perfil c3  de producción. Investigadores adscritos en el DEC yel DEP.

La mayoría de los investigadores en  c3 estan en Tijuana.

Entre los investigadores **NO** catedras  que estan en c3, el 56%,  (18 de 32),pertenecen a la  sede Tijuana.

¿Qué proporción representan los investigadores en c3 por departamento?


```{r, echo=FALSE,warning=FALSE,message=FALSE,fig.height = 6, fig.width =8}

idep<-read.csv("idep.csv")%>%
  rename(c3=type)%>%
  mutate(c3=ifelse(c3=="alta","No","Sí"))

ptd<-idep%>%
  ggplot(aes(fct_reorder(Departamento,p),p, fill = c3))+
  geom_bar(stat = "identity")+
  coord_flip()+
   theme_light()+
  labs(y="",x="",title="¿Qué proporción de investigadores están en C3?")+
  scale_fill_manual(values=c("#00BFC4",  "#F8766D"))+
  scale_y_continuous(labels=percent_format())+
 annotate("text", x="DEE", y=-.1, label="18%")+
annotate("text", x="DES", y=-.1, label="25%")+
  annotate("text", x="DEUMA", y=-.1, label="30%")+
  annotate("text", x="DEAP", y=-.1, label="33%")+
  annotate("text", x="DEC", y=-.1, label="53%")+
  annotate("text", x="DEP", y=-.1, label="47%")
ptd



ggsave("c3.jpg", width = 25, height = 17, units = "cm",dpi=300)
```

##### 4. Compromiso en docencia,  una área de oportunidad.

Para tener una perspectiva de la participación en docencia por sede la siguiente gráfica muestra la distribución de las horas impartidas para las 7 sedes.
```{r,echo=FALSE,warning=FALSE,message=FALSE}
tres<-read.csv('tres.csv')


mdocen<-tres%>%group_by( Sede)%>%
summarize(pts=mean(pts_prod),hrs=mean(Hrs_cls))

doc<-ggplot(tres, aes(reorder(Sede, Hrs_cls, FUN = median), Hrs_cls)) +
  geom_boxplot(aes(fill=Sede ))+
  theme_light()+
  labs(y="Horas clase", subtitle="Historico SIEVA 2017,2018,2019 ", x="Sede",title="Participación en Docencia")+
theme_classic(base_size = 14)+
theme(legend.position='none')+
  theme(axis.text.x = element_text(angle = 45, hjust = 1))

      ggplotly(doc)

ggsave("boxplot.jpg", width = 25, height = 17, units = "cm",dpi=300)
```



El rubro de docencia da cuenta de niveles críticos de compromiso por parte de los investigadores en cada sede. 


La sede Piedras Negras tiene el menor promedio de horas clase impartidas con 26.6 hrs lo que representa prácticamente 13 horas por año en promedio, le sigue Mexicali con 30, Matamoros con 53 horas,  Monterrey, Juárez, Tijuana y Nogales con  98 horas, el mayor nivel promedio entre las sedes de El Colef. 


No obstante, los diversos niveles de participación, es evidente que la impartición de clases representa una debilidad que se convierte en un área de oportunidad prioritaria.
